import { injectGlobal } from 'vue-styled-components';

injectGlobal(`
  * {
    color: white;
  }
`);
export const primary = '#B08BFF';
export const secondary = '#C891FF';

export default {
  primary,
  secondary,
};
